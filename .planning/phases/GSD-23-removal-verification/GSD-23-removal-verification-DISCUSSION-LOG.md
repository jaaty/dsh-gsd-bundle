# Phase 23: removal-verification — Discussion Log

Held a lightweight design interview on Phase 23 (removal-verification, DEGR-05). Oriented against STATE.md/ROADMAP, inspected lib/_capabilities.js, lib/_render.js, test/mount.test.mjs, and cordis.patch.yml. Findings: phase 22 deliberately deferred the systematic per-plugin removal suite (mount.test.mjs:436); phase 22 already proved rendering/routing degrade for ad-hoc subsets. Five grey areas surfaced and settled by the user: (1) step-plugin set = the 5 role:'step' loop plugins, with a cross-cutting note that the whole bundle should be swappable and the harness must be generic over the plugin rows so any row can be added; (2) retirement model = never-apply/subset mount; (3) end-to-end depth = coherence + offline-runnable smoke calls (discuss/plan/verify; execute/ship present+registered only); (4) effects-reverted = all six surfaces; (5) harness lives in test/removal.test.mjs importing a shared harness extracted to test/helpers/mount-harness.mjs. The swappability goal beyond the 5-step matrix is deferred to future milestone work.
