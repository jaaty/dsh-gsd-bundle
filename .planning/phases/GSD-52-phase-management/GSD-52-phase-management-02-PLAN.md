---
phase: 52-phase-management
plan: 02
type: execute
wave: 2
depends_on: ["GSD-52-phase-management-01"]
files_modified: ["lib/phase-management-plugin.js", "lib/_capabilities.js", "lib/commands.js", "package.json", "cordis.patch.yml", "test/phase-management-git.test.mjs"]
autonomous: true
requirements: ["CLH-01"]
gap_closure: false
user_setup: []
must_haves:
  truths:
    - "gsd_phase executes add/insert/remove/reorder/edit against .planning/ROADMAP.md and .planning/STATE.md; a successful mutation writes both files and lands exactly one atomic commit through commitArtifacts, and a failed hard check writes neither file (byte-identical on disk)."
    - "The gsdPhaseManagement capability advertises the gsd_phase tool and the /gsd-phase-manage command, so the command is only registered while the capability is present (DEGR-01/03)."
    - "ROADMAP's ## Progress table is regenerated by stringifyRoadmap and STATE progress is recomputed by s.recomputeProgress after every mutation (D-05); STATE active_phase is cleared when the active phase itself is removed."
  artifacts:
    - path: "lib/phase-management-plugin.js"
      provides: "The out-of-band gsd_phase loop-step plugin: capability publish, tool registration, action parse + fail-closed orchestration, atomic multi-file commit"
      min_lines: 100
      exports: ["name", "inject", "apply"]
    - path: "test/phase-management-git.test.mjs"
      provides: "FakeFs+gitFn integration tests: tool execute path, fail-closed atomicity, --yes guard, commitArtifacts capture"
      min_lines: 70
      exports: []
  key_links:
    - from: "lib/phase-management-plugin.js"
      to: "lib/phase-management.js"
      via: "imports applyPhaseAction for the in-memory CRUD mutation before any write"
      pattern: "applyPhaseAction"
    - from: "lib/phase-management-plugin.js"
      to: "lib/_git-artifacts.js"
      via: "imports commitArtifacts for the single atomic .planning commit (D-06)"
      pattern: "commitArtifacts"
    - from: "lib/phase-management-plugin.js"
      to: "lib/state.js"
      via: "calls s.recomputeProgress after writeRoadmap so STATE progress tracks ROADMAP (D-05)"
      pattern: "recomputeProgress"
---
<objective>
Surface the pure domain from Plan 01 as the interactive phase-CRUD tool: add lib/phase-management-plugin.js (the gsdPhaseManagement out-of-band capability + the gsd_phase tool that reads ROADMAP/REQUIREMENTS/STATE, parses the per-action flags, runs applyPhaseAction fail-closed, then writes ROADMAP, recomputes STATE progress, and commits atomically via commitArtifacts), register the /gsd-phase-manage command in commands.js plus its gsdPhaseManagement capability row in _capabilities.js, and wire the package.json export + cordis.patch.yml plugin row. Proven by a FakeFs+gitFn integration test suite.
</objective>
<context>@.planning/phases/GSD-52-phase-management/52-CONTEXT.md (decisions D-01..D-08), @lib/phase-management.js (the imported pure domain from Plan 01), @lib/health.js (lines 366-528: the tool+capability+commitArtifacts pattern to mirror), @lib/_capabilities.js (CAPABILITY_KEYS + TABLE), @lib/commands.js (COMMANDS array + apply), @lib/state.js (readRoadmap 440, readRequirements 450, readState 278, writeRoadmap 446, recomputeProgress 872, updateStateFrontmatter), @lib/_git-artifacts.js (commitArtifacts 174), @test/health.test.mjs and @test/phase-tools-git.test.mjs (integration-test style)</context>
<tasks>
  <task type="auto">
    <name>Task 1: Create lib/phase-management-plugin.js with the capability + gsd_phase tool and full fail-closed orchestration (tracer)</name>
    <files>lib/phase-management-plugin.js</files>
    <read_first>lib/health.js, lib/phase-management.js, lib/state.js, lib/_git-artifacts.js, #52-CONTEXT.md</read_first>
    <action>
      Create lib/phase-management-plugin.js mirroring lib/health.js's plugin shape (read lines 1-27 and 366-528 first). Set const name = "gsd-phase-management"; const inject = ["gsdState", "tools"]; do NOT inject or spawn any subagents (deterministic pure-JS path, DEGR-07 — mirrors health.js). Import { defineTool } from "@deepseek-ai/dsh-tools"; import { cwdOf } from "./_runner.js"; import { commitArtifacts } from "./_git-artifacts.js"; import { buildCapability } from "./_capabilities.js"; import { applyPhaseAction } from "./phase-management.js".
      Inside function apply(ctx): const gsd = () => ctx.get("gsdState"); call ctx.provide("gsdPhaseManagement", buildCapability("gsdPhaseManagement")) to publish the capability (DEGR-01). The buildCapability call requires the new capability row to exist in _capabilities.js (added in Task 2) so apply that _capabilities.js change together with this file before running the suite; the final state must pass.
      Register ctx.tools.register(defineTool({ ... })) with name "gsd_phase" and a description summarising the phase CRUD surface. parameters (schemas only): action { type:"string", enum:["add","insert","remove","reorder","edit"], required:true }; name { type:"string" }; goal { type:"string" }; requirements { type:"array", items:{ type:"string" } }; at { type:"number" }; n { type:"number" }; to { type:"number" }; status { type:"string", enum:["Complete","pending"] }; yes { type:"boolean" }. output: { schema: { type:"string" }, render: (_a,v)=>[{type:"text",text:v}] }. presentCall: a generic card titled "Phase management" with rawInput spread from the resolved flags.
      In async execute(args, exec): (1) const cwd = cwdOf(exec); const s = gsd(); throw when !s, and when !(await s.isProject(cwd)) throw "gsd_phase: no .planning/ project — run gsd_init first". (2) const roadmap = await s.readRoadmap(cwd); if (!roadmap) throw new Error("gsd_phase: ROADMAP.md is missing or unparseable") (D-04 ROADMAP must parse). (3) const reqs = await s.readRequirements(cwd); const reqIds = new Set(reqs.map(r=>r.id)); const stateDoc = await s.readState(cwd); const activePhase = stateDoc?.frontmatter?.active_phase ?? null. (4) Map args into an action object: for add/insert use { op:args.action, name:args.name, goal:args.goal, requirements:args.requirements || [], status:args.status, ...(args.action==='insert' ? { at:args.at } : {}) }; for remove/reorder/edit use { op:args.action, n:args.n, ...(args.action==='reorder' ? { to:args.to } : {}), ...(args.action==='edit' ? { name:args.name, goal:args.goal, requirements:args.requirements, status:args.status } : {}) }. (5) const res = applyPhaseAction(roadmap, action, { reqIds, activePhase, confirm: args.yes === true }); if (res.ok === false) throw new Error(`gsd_phase ${args.action} failed: ${res.error} (${res.code})`) — the fail-closed gate: NOTHING has been written yet (D-06). (6) On ok, await s.writeRoadmap(cwd, res.doc) which regenerates the ROADMAP phase table AND its ## Progress table via stringifyRoadmap (D-05). (7) If args.action === 'remove' and the removed phase's n === activePhase, immediately clear the dangling active phase via await s.updateStateFrontmatter(cwd, { active_phase: null, current_phase: null }) BEFORE recompute, so STATE active_phase never points at a removed phase (phase-goal integrity). (8) await s.recomputeProgress(cwd) to recompute STATE.frontmatter.progress.{total_phases,completed_phases,percent} from the roadmap (D-05). (9) await s.addDecision(cwd, `Phase management ${args.action}: ${res.doc.phases.length} phase(s) now`) mirroring the health addDecision pattern. (10) Commit atomically with const commit = await commitArtifacts(cwd, null, { scope: 'phase-management', message: `docs(planning): phase-management ${args.action} via gsd_phase` }) — pass phaseNum NULL and the message override (this is an out-of-band multi-phase mutation; D-06 single-commit). Do NOT call ensurePhaseBranch (out-of-band tool, mirrors undo/health's non-loop stance). (11) Return a summary string naming the action, the resulting phase count, and the committed file list (or warning) from the commit result.
    </action>
    <verify>grep "name: \"gsd_phase\"" lib/phase-management-plugin.js && grep "ctx.provide(\"gsdPhaseManagement\"" lib/phase-management-plugin.js && grep "applyPhaseAction" lib/phase-management-plugin.js && grep "recomputeProgress" lib/phase-management-plugin.js && grep "commitArtifacts" lib/phase-management-plugin.js && grep "updateStateFrontmatter" lib/phase-management-plugin.js
    </verify>
    <acceptance_criteria>
      - grep "gsd_phase" and "gsdPhaseManagement" both present in lib/phase-management-plugin.js (exit 0)
      - the tool's execute throws (not writes) when readRoadmap returns undefined
      - the fail-closed gate "res.ok === false" is checked before any writeRoadmap/updateStateFrontmatter/recomputeProgress/commitArtifacts call
      - commitArtifacts is invoked with a null phaseNum and a message override (grep "null, {" in the commitArtifacts call)
    </acceptance_criteria>
    <done>lib/phase-management-plugin.js publishes gsdPhaseManagement and registers gsd_phase with the full fail-closed orchestration: read -> parse -> applyPhaseAction -> writeRoadmap -> (clear dangling active) -> recomputeProgress -> addDecision -> one atomic commitArtifacts.</done>
  </task>
  <task type="auto">
    <name>Task 2: Register the capability row, the /gsd-phase-manage command, the package export, and the cordis patch row</name>
    <files>lib/_capabilities.js, lib/commands.js, package.json, cordis.patch.yml</files>
    <read_first>lib/_capabilities.js, lib/commands.js, package.json, cordis.patch.yml</read_first>
    <action>
      In lib/_capabilities.js (read lines 32-56 and 62-316 first): append "gsdPhaseManagement" to the CAPABILITY_KEYS frozen array (after "gsdAddTests"), and add a matching TABLE entry keyed gsdPhaseManagement with step:"phase-management", role:"out-of-band", tools:["gsd_phase"], commands:["gsd-phase-manage"], order: NOT_LOOP_ORDERED, prereq:[], next:[], produces:[], consumes:["ROADMAP.md","STATE.md"]. Keep buildCapability row shape identical to gsdHealth/gsdAddTests. This drives CAPABILITY_KEYS.length from 23 to 24.
      In lib/commands.js (read the COMMANDS array 35-370 and apply 372-406 first): add a COMMANDS entry for { name:"gsd-phase-manage", description, hint:"add|insert|remove|reorder|edit ...", build(raw) }. In build, parse the action word (raw.trim().split(/\s+/)[0]) and require it be one of add|insert|remove|reorder|edit (else return { err: "Usage: /gsd-phase-manage add|insert|remove|reorder|edit ..." }); then route via return { text: "Run the gsd_phase tool with action " + action + " and the parsed flags (name/goal/requirements/at/n/to/status/yes) from: " + raw.trim(), ack: "Phase management " + action + " → gsd_phase." }. Reuse the existing phaseNum(raw) helper convention only where relevant; this command has no leading phase number. Do not modify other COMMANDS entries.
      In package.json (read the exports block 34-114): add "./phase-management": { "default": "./lib/phase-management-plugin.js" } after the "./autonomous" entry (the "files" array already ships lib/*.js, so no files change needed).
      In cordis.patch.yml (read the insert list around lines 95-103): add a plugin row comment + `- id: gsd-phase-management` / `name: '@dsh-gsd/bundle/phase-management'` alongside the other insert rows (e.g. after gsd-health).
    </action>
    <verify>grep "gsdPhaseManagement" lib/_capabilities.js && grep "gsd-phase-manage" lib/commands.js && grep "phase-management" package.json && grep "gsd-phase-management" cordis.patch.yml
    </verify>
    <acceptance_criteria>
      - grep "gsdPhaseManagement" in lib/_capabilities.js CAPABILITY_KEYS and TABLE (exit 0)
      - grep "gsd-phase-manage" in lib/commands.js COMMANDS (exit 0)
      - grep "./phase-management" in package.json exports (exit 0)
      - grep "gsd-phase-management" in cordis.patch.yml (exit 0)
    </acceptance_criteria>
    <done>The capability row, slash command, package export, and cordis patch row all reference the new gsdPhaseManagement surface consistently.</done>
  </task>
  <task type="auto">
    <name>Task 3: Write the integration test suite for the tool execute path, fail-closed atomicity, and the --yes guard</name>
    <files>test/phase-management-git.test.mjs</files>
    <read_first>test/health.test.mjs, test/phase-tools-git.test.mjs, test/helpers/fake-fs.mjs, lib/phase-management-plugin.js</read_first>
    <action>
      Create test/phase-management-git.test.mjs using node:test, assert/strict, FakeFs, GsdState, and a fake gitFn capture (mirror the FakeFs + fake-ctx + initProject + makeExec harness used by test/health.test.mjs or test/phase-tools-git.test.mjs — read those first and reuse their helpers where possible). Build a project with initProject, seed a ROADMAP.md (2-3 phases, one Complete) with a matching REQUIREMENTS.md and STATE.md.
      Cover: (a) a successful add via the tool execute writes ROADMAP.md whose text contains the new phase row in the phase table AND a matching `## Progress` row, and STATE.progress.total_phases increments while percent recomputes from the roadmap (D-05); (b) insert at index 0 renumbers the following phase # to 02 in both tables; (c) remove of a pending non-active phase removes its rows from both tables and decrements total_phases; (d) remove of a Complete phase throws an error carrying the code, and afterwards both ROADMAP.md and STATE.md on disk are byte-identical to before the call (fail-closed atomicity, D-06); (e) remove of the active phase (STATE.active_phase set to that phase's n) without yes:true throws, and with yes:true succeeds AND STATE.active_phase is cleared to null; (f) edit toggles a pending phase to Complete and STATE.progress.completed_phases increments after recompute; (g) an unknown REQ-ID on add throws and neither file changes; (h) assert commitArtifacts was invoked exactly once per successful mutation with phaseNum null (capture the gitFn add/commit calls and assert the commit message contains "phase-management").
      Ensure the test only mocks ctx.fs writes via FakeFs and the injectable gitFn — never real git. Run the whole suite green.
    </action>
    <verify>node --test test/phase-management-git.test.mjs
    </verify>
    <acceptance_criteria>
      - node --test test/phase-management-git.test.mjs exits 0
      - grep the byte-identical fail-closed assertions (ROADMAP/STATE after a throwing call) in the test file (exit 0)
      - grep "active_phase" null-clearing assertion after yes:true active-phase removal in the test file (exit 0)
      - grep a commitArtifacts capture asserting the commit message contains "phase-management" (exit 0)
    </acceptance_criteria>
    <done>test/phase-management-git.test.mjs proves the gsd_phase tool end-to-end against FakeFs+gitFn: successful mutations write ROADMAP+STATE with recomputed progress and one atomic commit; hard failures leave both files byte-identical; the --yes confirm guard is honoured and the dangling active_phase is cleared.</done>
  </task>
</tasks>
