# Phase 5: window-ledger — Discussion Log

Discussed phase 5 (window-ledger) with the user. Established this is net-new (no WINDOWS.md / async-jobs anywhere). First explained what a 'window entry' is (one contiguous context-window of execution; WINDOWS.md reconstructs loop position after a reset) when the user asked, then re-asked. User chose: (1) append-only markdown ledger WINDOWS.md with per-window entries (WIN-<seq>, phase/step at open/close, timestamps, summary, optional CHECKPOINT-<PP> link); (2) async-jobs is a registry/manifest only (no job runtime), JSON list surfaced by gsd_status; (3) gsd_status gains two new sections ('## Windows' + '## Async Jobs') while keeping Session Continuity; missing files render 'no windows'/'no jobs' rather than error, corrupt files render a short warning not a crash; (4) WINDOWS.md entries link to phase-4 CHECKPOINT artefacts. WINDOWS.md at .planning/WINDOWS.md; manifest at .planning/async-jobs.json.
