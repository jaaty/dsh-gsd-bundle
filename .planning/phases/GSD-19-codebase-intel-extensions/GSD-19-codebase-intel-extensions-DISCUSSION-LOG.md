# Phase 19: codebase-intel-extensions — Discussion Log

Interviewed the human on the phase-19 grey areas (drift baseline, when drift is checked, ignore set, updater surface, changed-files→docs mapping, structured-answer surface, query-scoping semantics). Human selected all recommended options: stored snapshot manifest, auto check on every call, default ignore set, new gsd-intel-updater tool, heuristic map + updater confirmation, JSON output schema + text render, and targeted-exploration-only scoping. Sealed into decisions D-01..D-08.
