# Phase 9: job-runtime — Discussion Log

Discussed phase 9 (job-runtime) with the user. Established that the async-jobs manifest (phase 5) is registry-only — gsd_execute records a job row and marks it done in the same synchronous call; nothing runs in the background. The host subagents service returns a run immediately but the current spawnSubagent awaits it. User chose: (1) child-process jobs via node:child_process spawn (genuinely background, survive the tool call); (2) shell-command jobs only (argv + cwd), no subagent jobs this phase; (3) file-based result collection (.planning/jobs/<id>.result.json) that survives context resets; (4) lifecycle running → done/failed with started/finished timestamps, non-zero exit or thrown error marks failed with captured stderr/error, no timeout; (5) gsd_status shows each job's real running/done/failed state with id, kind, and result summary, replacing the registry-only 'No jobs'. Deferred: subagent background jobs, timeouts/cancellation, a dedicated gsd_job launch tool, retry/queueing.
