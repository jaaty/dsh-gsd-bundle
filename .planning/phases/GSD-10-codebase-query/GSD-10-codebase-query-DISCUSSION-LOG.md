# Phase 10: codebase-query — Discussion Log

Discussed phase 10 (codebase-query) with the user. Established that gsd_map_codebase is currently a one-shot write tool (spawns mapper agents that write 7 static docs to .planning/codebase/), and the deferred --query intel mode is the read side. User chose: (1) a fresh-context query subagent (reusing spawnSubagent) reads the existing map docs, then does targeted codebase exploration only where the map is silent, and returns a targeted answer; (2) 'without a full re-scan' means map-first + targeted/limited exploration (glob/grep for specific symbols/files), not unrestricted; (3) a single `query` string arg on gsd_map_codebase switches it into query mode, coexisting with (and ignoring) the existing fast/focus/paths/force flags; (4) if no map exists, return a clear notice to run gsd_map_codebase first (or pass force); if the query subagent fails or returns nothing, return a clear failure message; never throw; (5) return the targeted answer as plain text with a short 'Sources' section citing the map doc(s) and/or codebase file(s). Deferred: drift detection, targeted re-map/updater, structured answer object, query scoping to subtrees.
