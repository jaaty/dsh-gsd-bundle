# Phase 4: checkpoint-resume — Discussion Log

Discussed phase 4 (checkpoint-resume) with the user. Established that the executor prompt already carries checkpoint semantics (stop + return structured state) and planIndex already computes has_checkpoints; the gap is gsd_execute never consumes the checkpoint. User chose: (1) per-plan CHECKPOINT-<PP> artefact (YAML frontmatter: last_completed_task, reason, hashes) for persistence; (2) resume skips by the recorded last_completed_task index (tasks 1..N done, begin N+1), with the resumed executor told 'RESUME from checkpoint N'; (3) fail-loud on corrupt/out-of-range checkpoint state (no silent re-run from task 1), SUMMARY-takes-precedence-over-CHECKPOINT; (4) persist+resume only for human/decision checkpoints — the active conversational UAT loop is deferred to a separate later milestone (out of scope here).
