# Phase 8: capability-gates — Discussion Log

Discussed phase 8 (capability-gates) with the user. Established that the three gates are pure headless repo scans reusing existing prompt concepts: security scans changed files for the lib/_agents.js:283 secret pattern list; broken-windows scans changed files for unreferenced TODO/FIXME/XXX + skipped tests/stubs; TDD-audit verifies type:tdd plans follow test: before feat:/fix:. User chose: (1) concrete headless scans for each gate; (2) changed-files-only scope (git diff vs merge-base); (3) failing required gate blocks the ship with a structured report via fail(), plus a per-gate skip (config gates.<name>:false or CLI flag) reported as 'skipped'; (4) gates configurable via a config.json gates:{security,bool broken_windows:bool tdd_audit:bool} block defaulting true; (5) TDD-audit enforces on type:tdd plans regardless of global tdd_mode (repo has tdd_mode:false but ships type:tdd plans). All gates' pass/fail/skipped status is always reported.
